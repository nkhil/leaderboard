import { extendTheme } from "@chakra-ui/react"

export default extendTheme({
  colors: {
    brand: {
      100: "#bfdef2",
      'white': '#FFF',
    },
  },
  fonts: {
    heading: 'IBM Plex Sans',
    body: 'IBM Plex Sans',
  },
})
